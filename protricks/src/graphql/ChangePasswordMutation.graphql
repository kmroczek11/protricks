mutation ChangePassword($input: ChangePasswordInput!) {
  changePassword(changePasswordInput: $input){
    token
    user {
      id
      firstName
      lastName
      email
      imgSrc
      roles
    }
  }
}
