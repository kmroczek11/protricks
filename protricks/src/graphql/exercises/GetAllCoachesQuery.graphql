query GetAllCoaches {
  coaches {
    facebookUrl
    instagramUrl
    user {
      id
      firstName
      lastName
      imgSrc
    }
    city {
      id
      name
      room
      citySrc
      roomSrc
      mapSrc
      priceListSrc
    }
    groups {
      id
      name
      limit
      trainees {
        id
      }
    }
  }
}
