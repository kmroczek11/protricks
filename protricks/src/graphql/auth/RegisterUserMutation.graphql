mutation RegisterUser($input: RegisterUserInput!) {
  registerUser(registerUserInput: $input) {
    accessToken
    user {
      id
      firstName
      lastName
      email
      imgSrc
      roles
    }
  }
}
